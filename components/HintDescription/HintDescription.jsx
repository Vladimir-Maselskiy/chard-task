import { StyledHintDescription } from './HintDescription.styled';

export const HintDescription = ({ children }) => {
  return (
    <StyledHintDescription>
      {children}
    </StyledHintDescription>
  );
};
